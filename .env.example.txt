# --- Project BraytonCognizant Environment Template ---

# Neo4j AuraDB Cloud Configuration
NEO4J_URI=neo4j+s://your-auradb-instance-id.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_database_password_here

# AI Engine Orchestration Credentials
GROQ_API_KEY=gsk_your_groq_api_key_here

# SSL Certificate Trust Override
# Set to "true" if you encounter SSL certificate verification errors on your machine.
# This is safe to use with Neo4j AuraDB (cloud) as the connection is still encrypted.
# Default is "false" — only enable if needed.
NEO4J_TRUST_ALL=false